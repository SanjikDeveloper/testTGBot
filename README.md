# Telegram Bot Navigator 🤖

Telegram бот-навигатор для удобного открытия других ботов через интерактивное меню.

> [!TIP]
> **Хочешь понять, как всё устроено внутри?** Прочитай наш подробный гайд: [EXPLANATION.md](./EXPLANATION.md) — там разбор архитектуры, кода и структуры «на пальцах».

## Особенности

- ✅ **Clean Architecture** - чистая архитектура с разделением на слои
- ✅ **PostgreSQL** - надёжное хранение данных
- ✅ **Reply + Inline клавиатуры** - удобная навигация
- ✅ **Deep Links** - прямое открытие ботов в Telegram
- ✅ **Категории** - организация ботов по группам

## Быстрый старт

### 1. Установка зависимостей

```bash
go mod download
```

### 2. Запуск базы данных

#### Вариант А: Через Docker (Рекомендуется) 🐳
Убедитесь, что Docker Desktop запущен, и выполните:
```bash
docker-compose up -d
```
Это автоматически поднимет базу данных и применит все миграции.

#### Вариант Б: Локальный PostgreSQL
Используйте скрипт настройки (требует установленного `psql` в PATH):
```powershell
.\setup_database.ps1
```

### 3. Настройка .env

Убедитесь, что `.env` содержит правильные настройки:

```env
BOT_TOKEN=your_bot_token_here
ENV=LOCAL

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=telegram_bot_navigator
DB_SSLMODE=disable
```

### 4. Запуск

```bash
go run ./cmd/main.go
```

Или скомпилируйте и запустите:
```bash
go build -o bot.exe ./cmd/main.go
.\bot.exe
```

## Структура проекта

```
testTGBot/
├── cmd/                    # Точка входа
├── internal/
│   ├── models/            # Domain модели
│   ├── repository/        # Работа с БД
│   ├── service/           # Бизнес-логика
│   └── delivery/          # Telegram handlers
├── pkg/                   # Общие утилиты
├── migrations/            # SQL миграции
└── .env                   # Конфигурация
```

## Использование

1. Откройте бота в Telegram
2. Нажмите `/start`
3. Выберите "🤖 Открыть ботов"
4. Выберите категорию
5. Выберите бота
6. Нажмите "🚀 Открыть бота"

## Добавление новых ботов

```sql
INSERT INTO bots (username, display_name, description, category_id, icon, is_active) 
VALUES ('bot_username', 'Название бота', 'Описание', 1, '🤖', true);
```

## Технологии

- Go 1.25
- PostgreSQL 17
- [go-telegram/bot](https://github.com/go-telegram/bot) - Telegram Bot API
- [pgx](https://github.com/jackc/pgx) - PostgreSQL driver
- [env](https://github.com/caarlos0/env) - Environment config

## Лицензия

MIT
